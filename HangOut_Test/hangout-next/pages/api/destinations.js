export default function (req, res) {
    // requete vers base de donnees

    // traitement des donnees

    // renvoie de la reponse
    res.status(200).json({ destionations: ['Monaco', 'Paris', 'Marseille'] })
}